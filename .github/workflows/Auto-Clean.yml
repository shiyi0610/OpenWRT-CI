# SPDX-License-Identifier: MIT
# Copyright (C) 2026 VIKINGYFY

name: Auto-Clean

on:
  # 仅保留手动触发，不会自动运行
  workflow_dispatch:

permissions: write-all

jobs:
  auto_clean:
    runs-on: ubuntu-latest
    steps:
      - name: Auto-Clean disabled
        run: |
          echo "Auto-Clean is currently disabled."
          echo "No releases, tags, or workflow runs will be deleted."
